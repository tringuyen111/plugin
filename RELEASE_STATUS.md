# SDLC Intelligence 1.0.0 — local distributable release status

Lifecycle decision: `PROMOTED` for this versioned local distributable under the `ASSURED` profile at `ELEVATED` assurance after exact-package postconditions pass.
Projection: native multi-Skill Codex plugin.
Local Codex install/discovery: `PASS` on Codex CLI 0.147.0 for the exact released package. External publication: `NOT_RUN`.
Behavioral routing comparison: `DIRECTIONAL_PASS` under `SANDBOX_PROCEDURAL_COMPARISON`; independent Codex-model attestation: `NOT_RUN` and not claimed.
Git branch/HEAD/tag evidence: `UNAVAILABLE` because supplied source ZIPs contain no Git metadata.

## Native topology

- 71 first-class `skills/<name>/SKILL.md` entrypoints;
- no active `skills/sdlc-intelligence/SKILL.md` mega-router;
- exactly 32 implicit-visible Skills: the prior 30 specialists plus `sdlc` and `upgrade-sdlc-intelligence`;
- Delivery/System route ownership remains 34 Delivery routes + 6 System routes;
- `defaultPrompt` uses the canonical array shape and Skill picker metadata is complete/non-ellipsized.

Monitoring is required for invocation misses, false triggers, router overlap, specialist bypass, context cost, and bundled-helper regressions.
This status does not claim upstream Git publication, marketplace publication, independent superiority, or universal competence.
