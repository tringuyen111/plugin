interface:
  display_name: Runbook
  short_description: Create or update an operational runbook from verified procedures,
    triggers, checks, recovery, rollback, and ownership.
policy:
  allow_implicit_invocation: false
