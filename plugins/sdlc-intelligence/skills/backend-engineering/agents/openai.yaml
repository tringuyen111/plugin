interface:
  display_name: Backend Engineering
  short_description: Deep backend application/service execution under /implement with
    transaction, side-effect, failure and runtime proof.
policy:
  allow_implicit_invocation: false
