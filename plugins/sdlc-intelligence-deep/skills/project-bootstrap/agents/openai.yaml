interface:
  display_name: Project Bootstrap
  short_description: Reconcile project context and durable configuration without duplicating native project truth.
policy:
  allow_implicit_invocation: false
