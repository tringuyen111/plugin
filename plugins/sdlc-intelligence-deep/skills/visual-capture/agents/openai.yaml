interface:
  display_name: Visual Capture
  short_description: Capture state-aware visual evidence with explicit environment,
    viewport, actions, masks, hashes, and manifest.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
