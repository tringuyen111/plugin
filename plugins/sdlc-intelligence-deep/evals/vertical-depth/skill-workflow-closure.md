# Frozen Vertical-Depth Cases — Skill Workflow Closure

Evidence-State: `NOT_RUN`

Freeze-Note: Frozen with F4a before behavioral qualification.

## Claim

A first-class Skill keeps mandatory **universal** control edges in `SKILL.md`. A branch-specific control workflow may live in a directly triggered conditional reference when that branch is not part of ordinary execution; once triggered, that branch method must remain atomic, while deeper references may only deepen HOW. Late-evidence re-entry, branch return, and completion behavior must never depend on accidental reference discovery.

## Cases

1. **API late compatibility evidence**
   - Input: a supported client reveals that a previously bound compatibility assumption is false after transport work has begun, while an independent endpoint behavior remains proven.
   - Expected: reopen the affected caller-contract/mechanism/proof decisions and dependents only; preserve independent caller-visible proof.
   - Falsifier: keep stale compatibility conclusions or restart the entire API unit without dependency evidence.

2. **Backend runtime premise changes**
   - Input: real runtime inspection disproves the assumed delivery/retry responsibility after implementation work, while unrelated business behavior remains established.
   - Expected: reopen affected responsibility/mechanism/failure-proof work only and preserve independent truth.
   - Falsifier: continue on the false runtime premise or discard unrelated established work.

3. **Frontend shared/state premise changes**
   - Input: browser evidence shows the assumed state lifetime/shared component contract is false after a local implementation is prepared.
   - Expected: reopen dependent state/composition/runtime/proof decisions; preserve unrelated verified UI behavior.
   - Falsifier: patch around the stale premise or restart unrelated frontend work.

4. **Persistence writer/atomicity premise changes**
   - Input: inspection discovers a database-side writer or external effect that invalidates the previously bound writer inventory/atomicity scope.
   - Expected: reopen affected invariant/mechanism/migration/concurrency proof only; preserve independent durable evidence.
   - Falsifier: retain the old atomicity claim or invalidate unrelated persistence proof without dependency.

5. **Security bypass/policy premise changes**
   - Input: new source/runtime evidence reveals an alternate protected-sink path or changed approved policy that invalidates the current enforcement proof.
   - Expected: reopen affected enforcement/negative-proof decisions and dependents; preserve independent security proof.
   - Falsifier: claim closure from the stale threat surface or restart unrelated security work indiscriminately.

6. **Decision Interview shared-assumption invalidation without reference preload**
   - Input: two accepted decisions depend on one assumption, a third is independent, and a new fact falsifies the shared assumption before `challenge-and-reentry.md` has been loaded.
   - Expected: `SKILL.md` alone is sufficient to reopen the two dependents, preserve the third decision/evidence, and derive a new frontier; the reference is optional depth.
   - Falsifier: require the reference merely to know that selective re-entry is mandatory, keep stale dependents, or reopen the whole interview.
