# Creative Production Prompt / Knowledge Architecture — frozen cases

Evidence-State: `NOT_RUN`


These cases falsify F5b consumer claims. Structural assertions/package checks are not behavioral proof.

1. **Branch without directory exploration** — User asks for a logo direction. Expected: the Skill can select the Logo branch from `SKILL.md`, use the logo search + HOW module only when material, and return direction/rationale/scale constraints without browsing unrelated references.
2. **Knowledge RETURN integrates into workflow** — A CIP lookup returns styles/deliverables. Expected: the Agent integrates an identity-system direction + required touchpoints/constraints into the parent `FRAME/SELECT` state rather than dumping catalog rows.
3. **Single control authority** — Direction is unresolved. Expected: `SKILL.md` owns criteria -> diverge -> compare -> stress -> refine -> generate/inspect control; no second mandatory Creative Direction workflow must be loaded to discover those edges.
4. **Taxonomy ownership** — Logo/CIP/icon or slide taxonomy is needed. Expected: deterministic data/search is the bundled lookup representation; prose depth supplies distinct HOW/caveats rather than another active catalog copy.
5. **Slide boundary preservation** — Slide direction is requested. Expected: returned knowledge may include narrative arc, layout/content zones, chart/copy rationale, accessibility and visual treatment, but must not return CSS, implementation classes, animation classes, or frontend/presentation-engine mechanics.
6. **Icon boundary preservation** — A Product UI icon-system request arrives. Expected: Creative Production does not turn its icon corpus into Product UI semantics/implementation ownership; it returns to the boundary and uses Product Design/Frontend ownership as applicable.
7. **Volatile platform constraint** — A social/banner request depends on current platform dimensions/safe zones. Expected: bundled references remain advisory; current authoritative constraints are verified when material before the direction is finalized.
8. **No forced branch corpus** — A campaign poster has no matching branch-specific corpus. Expected: use the resident creative control model (and optional Design Intelligence only when it changes the decision) rather than forcing the request into logo/CIP/slides data.

Behavioral execution remains `NOT_RUN` until a reproducible model/runtime run exercises these cases on exact revisions.
9. **Deterministic retrieval does not own creative judgment** — A logo/CIP/slide request uses bundled local data. Expected: scripts may rank/search/format traceable corpus records, but the Agent compares evidence against current brand/message/context and owns style/layout/type/color/narrative selection. Falsifier: deterministic code emits a selected creative direction, recommendation, pattern-break, or full-bleed decision from hard-coded rules.
10. **No-match does not fabricate creative support** — Query bundled CIP/slide evidence with `zzzxxyyqq nonexistentsignal`. Expected: deterministic lookup returns no matching evidence (or bounded empty domains) and the Skill reasons from other authorized evidence/assumptions if appropriate. Falsifier: a no-match path emits `Corporate Minimal`, `feature-grid`, default typography/color, or another concrete direction as if corpus-supported.
11. **No duplicate presentation engine** — User requests a CIP/slide presentation artifact. Expected: Creative Production supplies bounded visual direction/evidence; the host presentation capability owns file/build mechanics when available. Falsifier: a hidden/dead Creative Production HTML presentation renderer acts as a competing presentation engine with hard-coded visual choices.
12. **Native discovery owns cross-capability continuation** — A request is actually Product UI design/review, visual QA, Prototype, or production implementation. Expected: Creative Production stops at the ownership boundary and returns the bounded unresolved concern; host-native discovery/invocation selects any subsequent capability. Falsifier: the Skill encodes `/product-design`, `/design-review`, `/verify-quality`, `/prototype`, `/frontend-engineering`, or `/design-intelligence` as a next-Skill route.
13. **Retained decision tables remain advisory evidence** — Slide layout/color/typography/background CSVs are useful. Expected: if retained, they are directly searchable/readable as advisory corpus evidence and weighed by Agent reasoning; they are not hidden behind deterministic decision functions or left as dead package files.
