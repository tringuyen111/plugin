interface:
  display_name: Upgrade SDLC Intelligence
  short_description: Route reusable SDLC-system maintenance to exactly one System
    owner from artifact state, evidence, and lifecycle need.
policy:
  allow_implicit_invocation: true
