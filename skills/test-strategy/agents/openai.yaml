interface:
  display_name: Test Strategy
  short_description: Design risk-based QA coverage for fixed scope, choosing verification
    levels, priorities, environments, and evidence.
policy:
  allow_implicit_invocation: true
