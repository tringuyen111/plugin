interface:
  display_name: Create Integration
  short_description: Create or revise a provider adapter with auth, side effects,
    provenance, fallback, and behavioral evaluation.
policy:
  allow_implicit_invocation: false
