interface:
  display_name: Product Definition
  short_description: Define a product outcome, success metric, priority, and feature
    scope from an evidence-grounded opportunity.
policy:
  allow_implicit_invocation: false
